using PkG  # just in case
try using Plotly
  catch
      Pkg.add("Plotly")
      using Plotly
  end

Plotly.set_credentials_file(Dict("username"=>"user.name","api_key"=>"1234567890"))

trace1 = scatter(
  x= [1, 2, 3, 4],
  y= [10, 15, 13, 17],
  mode="lines")
trace2 = scatter(x=[2,3,4,5], y=[10,15,13,17],type="scatter")
#[  "x" => [1, 2, 3, 4],  "y" => [16, 5, 11, 9],  "type" => "scatter"]
response = Plotly.plot(trace1)
#,opt=[filename: "basic-line", fileopt: "new"])
display(response)
